#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n, l, r;
        cin>>n>>l>>r;
        vector<int> arr;
        vector<int> inside;
        for(int i=0; i<n; i++){
            int temp;
            cin>>temp;
            if (i<r && i>=l) 
            inside.push_back(temp);
            
            else
            arr.push_back(temp);

        }
        rotate(inside.begin(), min_element(inside.begin(), inside.end()), inside.end());
        // for(auto i: inside)
        // cout<<i<<" ";
        // cout<<endl;
        int search = inside[0];

        auto it = arr.begin();
        while(it!=arr.end() && *it<search) it++;
        

        arr.insert(it, inside.begin(), inside.end());

        for(auto i: arr)
        cout<<i<<" ";
        cout<<endl;
        }
}
